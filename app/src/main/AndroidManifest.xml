<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Marks this as a watch-only APK so Play filters it correctly. -->
    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="true" />

    <!-- A few LTE-only / niche watches ship without a usable mic; this app is
         useless without one, so make it a hard requirement. -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- From API 30 on, a process that isn't foreground is handed silence by
         the microphone rather than an error. Capture therefore runs in a
         foreground service; uploads promote themselves the same way so a large
         transfer can outlive WorkManager's own execution budget. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Without this the ongoing notification is suppressed on API 33+, which
         would make a recording in progress invisible. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Wear aggressively suspends the CPU the moment the screen dims. The
         PARTIAL_WAKE_LOCK held while polling for a summary is what keeps the
         coroutine pipeline alive through a wrist-down gesture. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!-- Standalone: the watch app works without a paired phone companion. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting"
            tools:ignore="LockedOrientationActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Holds the microphone for the length of a take, so capture survives
             the Activity being stopped. -->
        <service
            android:name=".audio.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- WorkManager's own foreground service needs its type declared here
             for the ForegroundInfo UploadWorker supplies to be accepted. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
